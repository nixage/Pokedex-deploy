import { HidePokeInfoDirective } from './hide-poke-info.directive';

describe('HidePokeInfoDirective', () => {
  it('should create an instance', () => {
    const directive = new HidePokeInfoDirective();
    expect(directive).toBeTruthy();
  });
});
