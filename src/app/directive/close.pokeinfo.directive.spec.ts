import { Close.PokeinfoDirective } from './close.pokeinfo.directive';

describe('Close.PokeinfoDirective', () => {
  it('should create an instance', () => {
    const directive = new Close.PokeinfoDirective();
    expect(directive).toBeTruthy();
  });
});
