import { ShowPokeInfoDirective } from './show-poke-info.directive';

describe('ShowPokeInfoDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowPokeInfoDirective();
    expect(directive).toBeTruthy();
  });
});
